/* the ternary operator in js is a concise way to write conditional statements. its often used as a
shortcut for simple if-else statement, allowing you to make decisions and assign values to a variable 
based on a condition.*/
// these are the some syntax of the ternary operator;
// example = codition ? expressionIftrue : expressionIffalse;

// code for odd even using if-else statement
let number = 7;

let result;
if (number % 2 == 0)
{
    result = "Even";
}
else
{
    result = "Odd";
}
console.log('the result id ${result).');